node_modules/

# Extension signing keys — the public half lives in extension/manifest.json,
# the private half must never be committed. See README.
*.pem
*.pkcs8

# Packed builds. Regenerable, and a stale one ships whatever architecture it was
# packed from — never let one become the artifact of record.
*.crx

# Vercel / Next.js (proxy/)
.next/
.vercel/
.env
.env.*
!.env.example

.DS_Store
